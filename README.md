mac-like-chromebook-tab-nav
===========================

use the ChromeOS chrome.commands API to enable mac-like tab navigation with "alt+shift + {" and "alt+shift+}" instead of "ctrl + tab" and "ctrl + shift + tab"
